import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  eventItem: {
    backgroundColor: 'white',
    borderRadius: 5,
    padding: 10,
    marginRight: 10,
    marginTop: 17,
  },
  eventTitle: {
    fontWeight: 'bold',
  },
});
